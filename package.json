{
  "name": "custom-usdt",
  "version": "1.0.0",
  "description": "Custom USDT token with auto-burn and price oracle features",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:ethereum": "hardhat run scripts/deploy.js --network ethereum",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify": "hardhat verify",
    "hardhat": "hardhat"
  },
  "keywords": [
    "usdt",
    "erc20",
    "ethereum",
    "smart-contract"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.0",
    "hardhat-gas-reporter": "^1.0.9",
    "solidity-coverage": "^0.8.5",
    "@openzeppelin/hardhat-upgrades": "^2.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@chainlink/contracts": "^1.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0"
  }
}
